{% extends 'Front.html.twig' %}

{% block title %}Confirmation{% endblock %}

{% block body %}
<div class="service">
    <div class="container">
        <div class="text-center">
            <h1>Votre panier a été enregistré avec succès !</h1>
            <div style="margin-top: 50px;">
                <a href="#" class="btn btn-primary">Retour au menu</a>
                <a href="{{ path('app_panier_index') }}" class="btn btn-success">Afficher le total</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
