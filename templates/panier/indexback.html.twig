{% extends 'back.html.twig' %}

{% block title %}Panier index{% endblock %}

{% block body %}
    <h1>Panier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prod_name</th>
                <th>Quantity</th>
                <th>Price</th>
                <th>Date</th>
                <th>Panier_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for panier in paniers %}
            <tr>
                <td>{{ panier.id }}</td>
                <td>{{ panier.prodName }}</td>
                <td>{{ panier.quantity }}</td>
                <td>{{ panier.price }}</td>
                <td>{{ panier.date ? panier.date|date('Y-m-d') : '' }}</td>
                <td>{{ panier.panierId }}</td>
                <td>
                    <a href="{{ path('app_panier_showback', {'id': panier.id}) }}">Show</a>
                
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">No records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <!-- Bouton "Retour vers le catalogue" -->
    <div>
        <a href="{{ path('app_produits_indexback') }}" class="btn btn-primary">Retour vers le catalogue</a>
    </div>
{% endblock %}
