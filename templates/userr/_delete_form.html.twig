<form method="post" action="{{ path('app_userr_delete', {'pseudo': userr.pseudo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ userr.pseudo) }}">
    <button class="btn">Delete</button>
</form>
